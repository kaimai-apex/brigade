{
  "_comment": "Violations that predate the architecture rule. This list may only SHRINK — check-architecture.mjs fails if an entry here no longer violates anything, so fixing one forces removing it. Each entry is 'file::rule'. Currently empty: every known violation has been fixed.",
  "allowed": []
}
